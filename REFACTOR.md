# 🏗️ REFACTOR.md - 현업 수준 리팩토링 가이드

## 📁 새로운 프로젝트 구조

```
airplane_chime_timer/
├── src/                          # 소스 코드
│   ├── modules/                  # 모듈 구조
│   │   ├── background/           # 백그라운드 스크립트 모듈
│   │   │   ├── AudioManager.js   # 오디오 재생 관리
│   │   │   ├── AlarmManager.js   # 알람 관리
│   │   │   └── BadgeManager.js   # 브라우저 배지 관리
│   │   ├── popup/                # 팝업 UI 모듈
│   │   │   ├── controllers/      # UI 컨트롤러
│   │   │   ├── managers/         # 데이터 관리자
│   │   │   └── components/       # UI 컴포넌트
│   │   └── shared/               # 공통 모듈
│   │       ├── constants.js      # 애플리케이션 상수
│   │       ├── utils.js          # 유틸리티 함수
│   │       └── storage.js        # 스토리지 관리
│   ├── components/               # HTML 템플릿
│   ├── styles/                   # CSS 스타일
│   └── assets/                   # 리소스 파일
├── dist/                         # 빌드 결과물
├── config/                       # 설정 파일
├── scripts/                      # 빌드/개발 스크립트
├── tests/                        # 테스트 파일
├── package.json                  # 프로젝트 메타데이터
├── webpack.config.js             # 번들링 설정
├── .eslintrc.js                  # 코드 품질 설정
└── .prettierrc                   # 코드 포맷팅 설정
```

## 🛠️ 구현된 현업 수준 기능

### 1. **모듈화 시스템** ✅
- **공통 모듈 (`src/modules/shared/`)**
  - `constants.js`: 모든 상수를 중앙 관리
  - `utils.js`: 재사용 가능한 유틸리티 함수
  - `storage.js`: Chrome Storage API 래퍼 클래스

### 2. **개발 도구 설정** ✅
- **ESLint**: 코드 품질 및 일관성 검사
- **Prettier**: 자동 코드 포맷팅
- **빌드 스크립트**: 프로덕션 배포용 자동화

### 3. **타입 안전성** ✅
- **JSDoc**: 모든 함수와 클래스에 상세한 타입 정보
- **매개변수/반환값 문서화**: 개발자 경험 향상

### 4. **에러 처리** ✅
- **중앙 집중식 에러 관리**: 일관된 에러 메시지
- **로그 시스템**: 개발/프로덕션 환경 구분
- **사용자 친화적 알림**: 에러 상황 시 적절한 피드백

## 📦 패키지 관리

### 개발 환경 설정
```bash
npm install  # 의존성 설치
```

### 스크립트 명령어
```bash
npm run build       # 프로덕션 빌드
npm run build:dev   # 개발용 빌드
npm run watch       # 파일 변경 감지 및 자동 빌드
npm run lint        # 코드 품질 검사
npm run format      # 코드 포맷팅
npm test            # 테스트 실행
```

## 🧩 모듈 설계 원칙

### 1. **단일 책임 원칙**
- 각 모듈은 하나의 명확한 책임을 가짐
- `AudioManager`: 오디오 재생만 담당
- `StorageManager`: 데이터 저장/로드만 담당

### 2. **의존성 역전**
- 상위 레벨 모듈이 하위 레벨 모듈에 의존하지 않음
- 인터페이스를 통한 추상화

### 3. **모듈 간 느슨한 결합**
- 명확한 API를 통한 모듈 간 통신
- 메시지 패싱 패턴 활용

## 🎯 코드 품질 기준

### JavaScript 스타일 가이드
- **ES2021 모듈 시스템** 사용
- **const/let** 사용, var 금지
- **세미콜론** 필수
- **단일 따옴표** 사용
- **2스페이스 들여쓰기**

### 명명 규칙
- **클래스**: PascalCase (예: `AudioManager`)
- **함수/변수**: camelCase (예: `playSound`)
- **상수**: UPPER_SNAKE_CASE (예: `MESSAGE_TYPES`)
- **파일명**: PascalCase.js (예: `AudioManager.js`)

## 🔧 개발 워크플로우

### 1. **개발 환경 시작**
```bash
npm run dev  # 개발 서버 시작 + 파일 감시
```

### 2. **코드 작성**
- 모듈별로 기능 구현
- JSDoc으로 타입 정보 추가
- 에러 처리 로직 포함

### 3. **코드 검증**
```bash
npm run lint     # 코드 품질 검사
npm run format   # 자동 포맷팅
npm test         # 테스트 실행
```

### 4. **빌드 및 배포**
```bash
npm run build    # 프로덕션 빌드
# dist/ 폴더를 Chrome Extension으로 로드
```

## 📋 향후 개선 계획

### 단기 (1-2주)
- [ ] 나머지 모듈 분리 완성 (AlarmManager, BadgeManager)
- [ ] 컴포넌트 기반 UI 리팩토링
- [ ] 유닛 테스트 작성

### 중기 (1개월)
- [ ] TypeScript 마이그레이션
- [ ] 통합 테스트 환경 구축
- [ ] CI/CD 파이프라인 구축

### 장기 (3개월)
- [ ] 모니터링 시스템 도입
- [ ] 성능 최적화
- [ ] 국제화(i18n) 지원

## 🚀 현업 수준 특징

### 1. **확장성**
- 새로운 기능 추가가 용이한 모듈 구조
- 기존 코드 수정 없이 기능 확장 가능

### 2. **유지보수성**
- 명확한 책임 분리로 버그 추적 용이
- 일관된 코딩 스타일과 문서화

### 3. **테스트 가능성**
- 모듈 단위 테스트 가능한 구조
- 목킹(mocking) 용이한 설계

### 4. **협업 친화성**
- 명확한 개발 가이드라인
- 자동화된 코드 품질 검사
- 일관된 프로젝트 구조

---

## 💡 개발 팁

### 모듈 추가 시
1. `src/modules/` 하위에 적절한 위치 선택
2. JSDoc으로 타입 정보 필수 추가
3. 에러 처리 로직 포함
4. 단위 테스트 작성

### 디버깅 시
1. `log()` 함수 활용 (개발/프로덕션 환경 자동 구분)
2. Chrome DevTools 활용
3. 모듈 단위로 문제 범위 축소

### 성능 최적화
1. 불필요한 리렌더링 방지
2. 메모리 누수 방지 (이벤트 리스너 정리)
3. 번들 크기 최소화

이제 기존 레거시 코드를 점진적으로 이 구조로 마이그레이션하면 됩니다! 🎯
